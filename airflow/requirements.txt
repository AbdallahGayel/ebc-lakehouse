# Mirror of versions baked into the Airflow image. Keep in lockstep with
# airflow/Dockerfile so local Airflow installs (e.g. for CI) match runtime.
apache-airflow-providers-trino==5.7.2
trino[sqlalchemy]==0.330.0
dbt-core==1.8.7
dbt-trino==1.8.4
pyiceberg==0.8.0
pyarrow==16.1.0
boto3==1.34.0
requests==2.32.3
confluent-kafka==2.6.0
